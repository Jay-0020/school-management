# Keeps the registry directory in the repo. The real tenants.json (per-school DB
# URLs + JWT secrets) is written here by `npm run provision` and is gitignored.
# docker-compose.app.yml mounts this directory read-only into the app.
